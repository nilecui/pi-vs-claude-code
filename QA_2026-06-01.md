# QA 记录 · 2026-06-01

coms-net 控制面板(`apps/coms-dashboard`)本轮迭代的问答与处理记录。
分支:`coms-dashboard-redesign`(已合并 main)→ `coms-dashboard-terminals`(后续全部改动,未合并)。

---

## 1. 了解代码并启动网页版应用
- **Q**:了解代码帮我启动网页版应用。
- **A**:确认是 React + Vite + TS + Zustand 的 coms-net hub 控制面板。hub 已在 49840 运行,执行 `bun install` + `bun run dev`(5273),浏览器验证三栏渲染、连上 hub、3 个 agent 在线。

## 2. 浅色设计系统改造
- **Q**:前端深色不符合预期,参考 `design_system` 改样式。
- **A**:发现 `design_system`(AI-Employee 浅色设计语言,在仓库外的同级路径)。重写 `styles.css` 为浅色 token(白底、`#3b82f6` 主蓝、蓝→青渐变 logo、Geist 字体、玻璃卡片、三段式状态药丸),改 `GraphView` 配色 + Sidebar 渐变 logo。用 computed-style 实测全部命中设计系统。

## 3. 重设计(brainstorming → plan → 子代理执行)
- **Q**:重新设计:左加 agent 表单、更好看的 flow 图库推荐、节点点击设置、右侧可折叠日志、agent 互发、agent 设计模式有几种(参考 Pi-to-Pi 文档)。
- **A**:用 brainstorming(可视化 mockup)确认:布局 A 三栏常驻、图库 **AntV G6**、加 agent = 生成启动命令(零后端)、互发 = 编排式、节点面板四能力、4 种 agent 模式(子代理/队列/链式/点对点)。写 spec + 实现计划,subagent-driven 执行 12 个任务:G6 FlowGraph、AddAgentForm、AgentList、NodePanel、LogRail、三栏 App、store.orchestrate、launchCommand/orchestratePrompt 纯函数(单测)。移除 reactflow。
- **关键修复**:G6 在 React 18 StrictMode 下的销毁竞态、"Edge not found" 刷屏(改单 rAF 渲染器 + 渲染锁 + `animation:false`)。

## 4. 界面中文化 + 演示场景(后被移除)
- **Q**:界面中文,建立一个场景。
- **A**:全 UI 简体中文(共享 label 映射)。注入式 Prod/Dev PII 演示场景(seed,后扩展为 5 个 mock 场景)。**注:这些 mock 场景后续(第 18 条)已全部删除,换为真实运行器。**

## 5. G6 图美化
- **Q**:节点矩形太难看,改成圆形头像关系网 + 动画。
- **A**:圆形头像节点(身份色光环 halo + bot 图标 + 在线点 + 名字药丸)、曲线连边 + 每连接消息数徽标(store `edgeCounts`)、三种动画(消息粒子/蚂蚁线/光环呼吸,单 rAF 驱动)。

## 6. 图控制 + 布局
- **Q**:居中屏幕按钮、放大/缩小、默认树形;后续:连线太粗要细要带箭头;4 状态图例;回放按钮;适配不溢出。
- **A**:默认 `tree` 布局;`graph.zoomBy`/`fitView` 的放大/缩小/居中按钮;细化有向连线(`endArrow`);左下 4 状态图例(空闲/工作中/已停止/活跃连接,工作中=队列>0);回放按钮(`store.replay`,不重复计数);`fitView({when:"always"})` + Graph `padding:48` 防溢出。

## 7. 顶部终端条
- **Q**:日志改 mac 终端风格小窗、可折叠、运行中排前、流式、点击放大;红黄绿做成关闭/最小化/放大;全部最小化/展示;流式贴底滚动;合并为单一切换;默认单行横滑 + 拖拽调高换行 + 填满宽度。
- **A**:顶部 mac 终端条(交通灯标题栏 + 深色 shell 流);活跃优先排序;点击/绿灯放大模态(模态交通灯=关闭/最小化/全屏);红=关闭、黄=最小化;「全部最小化⇄全部展示」单一切换(仅展开时显示);打字机流式始终贴底;**网格 auto-fill 铺满宽度** + 底部把手**上下拖拽调高**、按高度自动多行。

## 8. 边点击 → 对话框
- **Q**:点线条上的对话数字弹出对话框;为什么对话框只显示 1 条。
- **A**:点 G6 边 → `ConversationDialog` 按连接过滤 `lines`。修复:`response` 行之前没写 `to`,补 `to:"dashboard"` 后对话框能同时显示提问 + 回复。

## 9. 模型接入(MiniMax + Codex 订阅)+ 真实运行
- **Q**:加入模型切换 minimax;另外加本地已登录的 Codex 订阅模型;看真实效果;新建的智能体加不进来吗。
- **A**:
  - 表单加预设 **MiniMax-M2.7**(`--provider minimax --model MiniMax-M2.7`,key 写入 gitignore 的 `.env`)和 **Codex 订阅 gpt-5.5**(`--provider openai-codex`,用本地 `~/.pi/agent/auth.json` 已登录的订阅,免 key)。`--print` 实测两者都真实返回。
  - 「加不进来」真相:浏览器不能拉起进程、pi 是 TUI headless 注册不上;且 pi 0.78 占用了 `--name` 核心 flag → 修 `extensions/coms-net.ts` 从 argv 读 `--name`,命名 agent 正常显示。
  - **一键启动 spawner**:`scripts/agent-spawner.ts`(tmux PTY,127.0.0.1)+ Vite `/spawner` 代理 + 表单「▶ 启动」+ `just spawner`。实测面板一键起真实 codex agent、注册进池、面板发消息→真实模型回复→firehose 回流面板。
  - 修 G6 `onUpdate` 控制台报错(`animation:false`)。

## 10. 群发 + 多选编排
- **Q**:如何发消息给多选的 agent;「让 cwt 去联系」要多选。
- **A**:左栏「群发消息」(多选 chip,面板→每个 agent);节点面板「让 X 去联系」改为多选 chip(X→多个 agent 各发一次编排)。

## 11. 观察者 firehose
- **Q**:问了 agent-WVQ9H2 但它窗口没日志,什么原因;重启。
- **A**:根因 = hub 没开 `PI_COMS_NET_OBSERVER_FIREHOSE`,peer↔peer 消息不镜像给观察者。用「复用现有 token + 钉同端口 49840 + 开 firehose」重启 hub(面板/agent 连接不受影响),agent 自动重连。

## 12. 清理无用 agent
- **Q**:为什么一直没回复;把没职责的 agent 删除。
- **A**:诊断:`agent-0VRTQ1/9TG3DV/WVQ9H2` 在线但 ctx 0%、队列堆积 = 僵尸会话(其实是早先 `just coms1/3/4` 起的、不处理消息的进程)。结束这些进程 + 注销,池中只留有职责的真实 agent。

## 13. 层级模式流程图
- **Q**:生成这种(层级)模式的流程图。
- **A**:用 excalidraw-diagram 技能产出 `docs/diagrams/hierarchy-orchestration.excalidraw`(+ PNG):控制面板→boss→lead-a/lead-b 的扇出+汇聚流程,含图例与真实编排 prompt 证据框,渲染校验通过。

## 14. 真实业务场景(标书 + 合同审查)
- **Q**:再设计两个可解释、能落地、工程化的真实业务场景(如标书制作),不是 demo。
- **A**:设计 + 实现 `lib/realScenarios.ts` + `ScenarioRunner`(后并入右侧聊天):
  - **标书制作**:`bid-lead` 拆 RFP → `tech-writer`/`commercial` 并行 → `compliance` 校验废标项 → 整合(层级+校验)。
  - **合同审查**:`legal-lead` → `intake` 提取 → `risk-reviewer`↔`redline-drafter` 互检 → 汇总意见书(peer+互检)。
  - **层级编排(演示)** 保留为基础形态。每个场景:选场景 + 粘贴输入 + 一键起真实 Codex agent + lead 真实编排。**先删除了全部 mock 场景**(`scenarios.ts`/`ScenarioMenu`/store 的 `runScenario/clearDemo/demoAgents`)。
  - 真实层级实测跑通:boss→lead-a/lead-b 真实下发、真实回答、firehose 回流面板。

## 15. 结果产出在哪
- **Q**:调用标书后最终生成结果在哪体现。
- **A**:加「📄 查看产出」弹窗:把 lead 报回控制面板的最终成果(标书大纲+废标项自检表 / 审查意见书)完整、可复制展示。实测 boss 回复后按钮出现、弹窗显示全文。

## 16. 使用说明文档
- **Q**:把各场景使用说明生成文档。
- **A**:`docs/scenarios-usage.md`:准备(hub+firehose / spawner / 凭证)、通用流程、三场景详解(角色/输入/流程/产出/为什么多 agent/工程化)、其他面板能力、真实 vs 演示说明。

## 17. 右侧聊天面板 + Markdown
- **Q**:业务场景放右边、要对话框、用好用的大模型对话控件、结果 Markdown 渲染。
- **A**:三栏布局(左工具栏 | 中关系图 | 右场景对话)。新 `ScenarioChat`:选场景 + 聊天 composer + 用 **react-markdown + remark-gfm** 渲染 lead 产出(表格/列表/代码/引用)。移除旧 `ScenarioRunner`。

## 18. 流式输出
- **Q**:需要流式输出。
- **A**:coms-net 是一次性返回完整响应(无 token chunk),故用**打字机式逐字渐显**模拟(`StreamingMarkdown` + 闪烁光标)。实测气泡文本 8→36→62→86 渐长、带光标、渲染 Markdown 列表。

## 19. 看着像卡住 / 没调 team / 中间会话
- **Q**:是不是卡住了?这是自己完成的吗?为什么没调 team?希望中间会话也有输出。
- **A**:
  - boss 没崩,它自己写完了内容(终端可见),但旧对话框只显示「你↔lead 主线」且其回复未及时回流 + 无「处理中」提示,所以像卡住。
  - 没调 team:因为这是**追问**(原样发给 lead,不含分派指令)+ 选的是「层级演示」而非「标书制作」。**团队分派只在场景首次下发(kickoff)发生**。
  - 已改:对话框显示**整条团队会话**(lead↔下属往返,带 `from→to` 标注、下属浅色)+ 加「正在处理」三点动画指示。

## 20. 本记录
- **Q**:以上所有记录写入 `QA_{date}.md`。
- **A**:即本文件 `QA_2026-06-01.md`。

---

## 运行方式(现状)
```bash
PI_COMS_NET_OBSERVER_FIREHOSE=1 just coms-net-server   # hub(开 firehose)
just spawner                                           # 一键启动服务(PTY)
cd apps/coms-dashboard && bun run dev                  # 面板 http://localhost:5273
```
- 真实模型:Codex 订阅(`pi` 内 `/login` 选 ChatGPT Codex,免 key)/ MiniMax(`.env` 里 `MINIMAX_API_KEY`)。
- 详细用法见 `docs/scenarios-usage.md`;层级流程图见 `docs/diagrams/`。

## 待办 / 可选优化(第一轮记录时的,后续多已完成)
- 清理累积的重复 agent + 场景运行前去重 → 第二轮「确定性编排 + 只 spawn 缺失角色」已解决。
- 助手气泡复制按钮;真·token 流(待 hub 支持分块响应)。
- 分支合并 → 见第二轮 P0.7(已合并 main)。

---

# 第二轮 · 平台化(配置驱动 → 多用户平台)

接「确认场景是否真调模型 / 不是 demo」之后,用户提出「必须硬编码吗?能不能泛化、加数据库、做平台级产品」。据此把 coms-dashboard 从 3 个硬编码场景演进为完整的**自托管多用户多 Agent 编排平台**。全程 brainstorm → spec → plan → 实现 → 测试 → 实跑验证。

## P0. 过渡:确定性编排 + 完整产出(合并入 5fc5298 / 2b2c57d 之上)
- **Q**:场景是真调模型吗?为什么 boss 自己干、没调 team?
- **A**:诊断 = lead 靠 coms 工具「自觉」分派不可靠(自己答完 / 互相转发死锁)。改为**确定性编排**:面板代码逐跳 `ask(角色)`→等回复→喂下一跳;每步加「禁止转发」破死锁;**按 msg_id 精确关联**回复(对多客户端鲁棒,实测抓出并修掉「我并发的 Playwright 会话污染」);只 spawn 缺失角色去重。
- **Q**:标书没内容?只有大纲?→ **A**:面板**无损装配**完整全文(大纲+技术正文+商务正文+合规),「📄查看完整产出」。实跑层级场景 lead-a‖lead-b→boss 真实跑通。

## P1–P7. 平台子项目(spec/plan 见 `docs/superpowers/{specs,plans}/`)
| 轮 | 子项目 | 关键产物 / 提交 |
|---|---|---|
| P1 | **A 配置驱动编排内核** | `src/lib/orchestration/`(types/template/validate/runScenario/scenarios);场景变纯数据 `ScenarioDef`;28 单测。`063b66e…f3d71a7` |
| P2 | **B 后端+SQLite+服务端编排** | `server/`(index/db/hubClient/agents/runHub/runner);编排移到服务端;实跑修真 bug(**hub 会话无心跳被回收**→`ask` 卡死)。`b88d915…30f9bd1` |
| P3 | **C 场景编辑器 UI** | `scenarioDraft.ts`(纯草稿变换)+ `ScenarioEditor`(表单式建/改/删 + 实时校验)。`7551a72 / a2c5f8a` |
| P4 | **E 运行历史 UI** | `RunHistory`(列表+重看产出+重跑,复用 runWith)。`ad0c098` |
| P5 | **D1 账号+归属隔离** | `auth.ts`(Bun.password+session)+ 鉴权中间件 + owner 隔离 + 登录门禁;`COMS_SERVER_HOST`。`9aeb530` |
| P6 | **D2 团队+共享** | `teams.ts` + `scenarios.team_id` 可见性 + 团队 API + Teams 模态 + 编辑器可见性下拉。`2f062e2 / 4592d9d` |
| + | **并发上限** | 就绪批次 `mapLimit`(默认 6),防 50-agent 宽扇出洪峰;`maxConcurrency` 可调。`d160b85` |

## P0.7 合并主干
- **Q**:合并分支 → **A**:`coms-dashboard-orchestration-core`(A+B)ff 合入 `coms-dashboard-terminals`;最终 `coms-dashboard-terminals → main` fast-forward(63 提交,68 文件 +9025/−286)。**未 push remote**(待用户确认)。

## 现状
- **81 单测全绿**、tsc 干净、build 成功;均在 `main`。
- 真实账号(测试):`alice / pw1`;开放注册。运行:`PI_COMS_NET_OBSERVER_FIREHOSE=1 just coms-net-server` + `just server` + `bun run dev`。
- 平台总览见 `apps/coms-dashboard/README.md`。
- **决策默认值**:自托管单主机多用户;鉴权=密码+httpOnly session cookie;共享=团队可见性;成员=owner 按用户名加;run 仍各人私有;并发默认 6。

## 待办 / 可选(平台之后)
- `push origin main` / 建 PR(对外动作,待确认)。
- D 之外:邀请码/SSO、run 团队可见、可视化 DAG 编辑器、容器化多租户(超出单主机定位)。
